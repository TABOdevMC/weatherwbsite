const $=id=>document.getElementById(id);const status=$('status'),weather=$('weather');
const codes={0:['☀️','Ciel dégagé'],1:['🌤️','Principalement dégagé'],2:['⛅','Partiellement nuageux'],3:['☁️','Couvert'],45:['🌫️','Brouillard'],48:['🌫️','Brouillard givrant'],51:['🌦️','Bruine légère'],53:['🌦️','Bruine'],55:['🌧️','Forte bruine'],61:['🌧️','Pluie légère'],63:['🌧️','Pluie'],65:['🌧️','Forte pluie'],71:['🌨️','Neige légère'],73:['🌨️','Neige'],75:['❄️','Forte neige'],80:['🌦️','Averses'],81:['🌦️','Averses'],82:['⛈️','Fortes averses'],95:['⛈️','Orage'],96:['⛈️','Orage avec grêle'],99:['⛈️','Orage avec grêle']};
async function geocode(name){const r=await fetch(`https://geocoding-api.open-meteo.com/v1/search?name=${encodeURIComponent(name)}&count=1&language=fr&format=json`);const d=await r.json();if(!d.results?.length)throw Error('Ville introuvable');return d.results[0]}
async function load(place){status.textContent='Chargement de la météo…';weather.hidden=true;try{const q=new URLSearchParams({latitude:place.latitude,longitude:place.longitude,current:'temperature_2m,relative_humidity_2m,apparent_temperature,precipitation,weather_code,wind_speed_10m',hourly:'temperature_2m,weather_code,precipitation_probability,uv_index',daily:'weather_code,temperature_2m_max,temperature_2m_min,uv_index_max',forecast_days:'7',timezone:'auto'});const d=await (await fetch(`https://api.open-meteo.com/v1/forecast?${q}`)).json();render(place,d);status.textContent='';weather.hidden=false}catch(e){status.textContent='Erreur : '+e.message}}
function render(p,d){const c=d.current,code=c.weather_code;const info=codes[code]||['🌡️','Conditions inconnues'];$('place').textContent=`${p.name}${p.country_code?' · '+p.country_code:''}`;$('temperature').textContent=`${Math.round(c.temperature_2m)}°C ${info[0]}`;$('description').textContent=info[1];$('feels').textContent=Math.round(c.apparent_temperature)+'°C';$('maxmin').textContent=`${Math.round(d.daily.temperature_2m_max[0])}° / ${Math.round(d.daily.temperature_2m_min[0])}°`;$('humidity').textContent=c.relative_humidity_2m+'%';$('wind').textContent=Math.round(c.wind_speed_10m)+' km/h';$('rain').textContent=Math.round(d.hourly.precipitation_probability[0])+'%';$('uv').textContent=d.daily.uv_index_max[0].toFixed(1);let now=new Date(c.time);let hs='';for(let i=0;i<24;i++){const t=new Date(d.hourly.time[i]),inf=codes[d.hourly.weather_code[i]]||['🌡️',''];hs+=`<div class="hour"><small>${t.toLocaleTimeString('fr-FR',{hour:'2-digit',minute:'2-digit'})}</small><div>${inf[0]}</div><b>${Math.round(d.hourly.temperature_2m[i])}°</b><small>☔ ${d.hourly.precipitation_probability[i]}%</small></div>`}$('hourly').innerHTML=hs;let ds='';for(let i=0;i<7;i++){const inf=codes[d.daily.weather_code[i]]||['🌡️',''];const dt=new Date(d.daily.time[i]);ds+=`<div class="day"><small>${dt.toLocaleDateString('fr-FR',{weekday:'short',day:'numeric'})}</small><div>${inf[0]}</div><b>${Math.round(d.daily.temperature_2m_max[i])}°</b><small>${Math.round(d.daily.temperature_2m_min[i])}°</small></div>`}$('daily').innerHTML=ds}
$('searchForm').addEventListener('submit',async e=>{e.preventDefault();const n=$('searchInput').value.trim();if(n)load(await geocode(n))});$('locationBtn').onclick=()=>navigator.geolocation.getCurrentPosition(p=>load({name:'Ma position',latitude:p.coords.latitude,longitude:p.coords.longitude}),()=>status.textContent='Position inaccessible.');load({name:'Paris',latitude:48.8566,longitude:2.3522,country_code:'FR'});